# Roadmap

Incremental symbol graph persistence, richer language parsers, semantic code search, signed plugin manifests, editor adapters, optional encrypted local state, reproducible desktop bundles, and policy packs are the next layers.
