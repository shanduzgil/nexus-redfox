# Release

Release builds should be created by the GitHub Actions workflow. The workflow builds source and wheels for supported Python environments and publishes checksums. Public repositories can use GitHub artifact attestations to establish build provenance.
