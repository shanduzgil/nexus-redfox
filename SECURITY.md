# Security Policy

Report vulnerabilities privately to the project owner before public disclosure. Do not include live credentials. Include the affected version, reproduction steps, impact, and a minimal proof when safe.

The default network surfaces are loopback-only. The scan engine does not execute target repository code. Agent access to remote endpoints is opt-in.
