# Contributing

Use Python 3.11 or newer. Keep behavior deterministic and local-first. Add tests for parser, scanner, graph, or protocol changes. Do not add secrets or real credentials. Keep source files free of inline comments and placeholder markers.

Run `python -m unittest discover -s tests -v`.
